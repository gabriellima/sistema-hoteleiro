<h1>Listing rooms</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Type</th>
    <th>Price</th>
    <th>Air conditioner</th>
    <th>Fireplace</th>
    <th>Number of rooms</th>
    <th>Balcony</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @rooms.each do |room| %>
  <tr>
    <td><%= room.name %></td>
    <td><%= room.class.name %></td>
    <td><%= room.price %></td>
    <td><%= room.air_conditioner %></td>
    <td><%= room.fireplace %></td>
    <td><%= room.number_of_rooms %></td>
    <td><%= room.balcony %></td>
    <td><%= link_to 'Show', room %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New search', new_search_path %>

